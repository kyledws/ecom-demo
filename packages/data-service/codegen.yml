overwrite: true

config:
  avoidOptionals: true
  immutableTypes: true
  skipTypename: true
  preResolveTypes: true

generates:
  src/generated/schema/index.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"

schema: "src/apis/type-defs.ts"

require:
  - ts-node/register
